package mekanism.fabric_shim.common.data;

/**
 * Compile-time stub (stand-in for NeoForge's datagen ExistingFileHelper). Datagen runs on the
 * NeoForge branch; api datagen helper signatures reference this type but never use it at runtime
 * on Fabric.
 */
public class ExistingFileHelper {

    private ExistingFileHelper() {
    }
}
